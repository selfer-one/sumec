{
  "name": "autoscaler-test",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "test": "mocha test_app.js --reporter mocha-junit-reporter --reporter-options mochaFile=results/test-results.xml"
  },
  "dependencies": {
    "axios": "^1.7.0",
    "chai": "^4.4.1",
    "minimist": "^1.2.8"
  },
  "devDependencies": {
    "mocha": "^11.0.0",
    "mocha-junit-reporter": "^2.2.0"
  }
}

